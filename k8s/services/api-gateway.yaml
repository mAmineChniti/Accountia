apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: accountia
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: accountia-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: accountia/api-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: KEYCLOAK_ISSUER_URI
              valueFrom:
                configMapKeyRef:
                  name: accountia-config
                  key: KEYCLOAK_ISSUER_URI
            - name: KEYCLOAK_JWK_SET_URI
              valueFrom:
                configMapKeyRef:
                  name: accountia-config
                  key: KEYCLOAK_JWK_SET_URI
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: accountia-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: accountia-config
                  key: REDIS_PORT
            - name: REPORTING_MS_URL
              valueFrom:
                configMapKeyRef:
                  name: accountia-config
                  key: REPORTING_MS_URL
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: accountia
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: accountia-platform
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
