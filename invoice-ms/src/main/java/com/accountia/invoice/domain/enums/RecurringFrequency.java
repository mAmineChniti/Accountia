package com.accountia.invoice.domain.enums;

/**
 * How often a recurring invoice schedule generates a new invoice.
 */
public enum RecurringFrequency {
    DAILY,
    WEEKLY,
    MONTHLY,
    QUARTERLY,
    YEARLY
}
