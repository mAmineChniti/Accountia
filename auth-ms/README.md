# auth-ms

Authentication microservice for Accountia.

Run locally with:

```bash
./mvnw -pl auth-ms spring-boot:run
```

Default sample user: `admin@accountia.test` / `password` (created on empty DB).
